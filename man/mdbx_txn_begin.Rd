% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/txn.R
\name{mdbx_txn_begin}
\alias{mdbx_txn_begin}
\title{Begin a transaction}
\usage{
mdbx_txn_begin(env, write = FALSE, flags = NULL)
}
\arguments{
\item{env}{An \code{mdbx_env} object, from \code{\link[=mdbx_env_open]{mdbx_env_open()}}.}

\item{write}{If \code{TRUE}, start a read-write transaction; the environment must
not have been opened with \code{readonly = TRUE}.}

\item{flags}{A character vector of 'libmdbx' transaction flag names, or
\code{NULL} for none. \code{"NOMETASYNC"} and \code{"NOSYNC"} relax durability for this
one transaction, exactly as the environment flags of the same names do for
every transaction — see the Durability section of \code{\link[=mdbx_flags]{mdbx_flags()}}.
\code{"TRY"} fails with \code{MDBX_BUSY} rather than waiting for another process's
writer. All three apply to write transactions only.}
}
\value{
An \code{mdbx_txn} object.
}
\description{
Starts a transaction against an open environment. Everything read or written
happens inside one: reads see a consistent snapshot taken when the
transaction began, and writes become visible to others only on commit.
}
\details{
'libmdbx' binds a transaction to the thread that started it, so an
environment supports \strong{one transaction at a time per thread} — and R is
single-threaded, so that means one at a time. Beginning a second while one is
open is an error, never a deadlock or a hang. Distinct environments are
independent, and concurrency comes from separate processes: many readers and
one writer may hold transactions on the same environment simultaneously.

A transaction must be ended with \code{\link[=mdbx_txn_commit]{mdbx_txn_commit()}} or \code{\link[=mdbx_txn_abort]{mdbx_txn_abort()}}. One
abandoned to the garbage collector is aborted, but that is a backstop rather
than a plan: a live write transaction holds the writer lock until it ends.
\code{\link[=mdbx_with_read]{mdbx_with_read()}} and \code{\link[=mdbx_with_write]{mdbx_with_write()}} handle this for you.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

txn <- mdbx_txn_begin(env)
txn
mdbx_txn_abort(txn)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_txn_commit]{mdbx_txn_commit()}}, \code{\link[=mdbx_txn_abort]{mdbx_txn_abort()}}, \code{\link[=mdbx_with_write]{mdbx_with_write()}},
\code{\link[=mdbx_flags]{mdbx_flags()}}, \link{mdbx-concurrency}
}
