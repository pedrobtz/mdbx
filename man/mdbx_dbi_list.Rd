% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbi.R
\name{mdbx_dbi_list}
\alias{mdbx_dbi_list}
\title{List the named databases in an environment}
\usage{
mdbx_dbi_list(txn, as = c("character", "raw"))
}
\arguments{
\item{txn}{An \code{mdbx_txn} object, from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}

\item{as}{\code{"character"} (the default) to decode names as UTF-8 text, or
\code{"raw"} for a list of raw vectors.}
}
\value{
A character vector of names, or a list of raw vectors if
\code{as = "raw"}. Empty when the environment has only the main database.
}
\description{
Reports the named databases visible to this transaction, which is not the
same as the ones it could open: a database created by a transaction that has
not committed is not listed, and one deleted but not yet committed still is.
}
\details{
Names are bytes, like keys, so a name that is not valid UTF-8 text needs
\code{as = "raw"}. The unnamed main database is not listed, having no name.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path, max_dbs = 8)

mdbx_with_write(env, function(txn) {
  mdbx_dbi_open(txn, "files", create = TRUE)
  mdbx_dbi_open(txn, "metadata", create = TRUE)
})

mdbx_with_read(env, function(txn) mdbx_dbi_list(txn))

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}}
}
