% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/txn.R
\name{mdbx_txn_abort}
\alias{mdbx_txn_abort}
\title{Abort a transaction}
\usage{
mdbx_txn_abort(txn)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object, from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}
}
\value{
\code{NULL}, invisibly.
}
\description{
Discards the transaction's writes and ends it. Aborting is idempotent: doing
it to an already-finished transaction does nothing, so it is safe to register
with \code{on.exit()} alongside an explicit \code{\link[=mdbx_txn_commit]{mdbx_txn_commit()}}.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

txn <- mdbx_txn_begin(env, write = TRUE)
mdbx_txn_abort(txn)
mdbx_txn_abort(txn) # already aborted; does nothing

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}, \code{\link[=mdbx_txn_commit]{mdbx_txn_commit()}}
}
