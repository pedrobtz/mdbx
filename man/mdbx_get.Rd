% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{mdbx_get}
\alias{mdbx_get}
\title{Read a value}
\usage{
mdbx_get(txn, key, default = NULL, as = c("character", "raw"), db = NULL)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object, from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}

\item{key}{A raw vector, or a single string, which is stored as its UTF-8
bytes. The two are interchangeable: \code{"k"} and \code{charToRaw("k")} name the
same key. Only raw can express a key containing a NUL byte.}

\item{default}{Value returned when \code{key} is absent. It is returned exactly as
given, and is never decoded, whatever \code{as} is set to.}

\item{as}{\code{"character"} (the default) to decode the stored bytes as UTF-8
text, or \code{"raw"} to return them untouched. Decoding fails, rather than
returning something corrupt, if the value contains a NUL byte or is not
valid UTF-8 — which is what any non-text value will do.}

\item{db}{An \code{mdbx_dbi} object from \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} naming a database to
address instead of the unnamed main one, or \code{NULL} for the main database.}
}
\value{
A length-1 character vector — or a raw vector if \code{as = "raw"} — or
\code{default} if the key is not present.
}
\description{
Looks up \code{key} in the transaction's snapshot.
}
\details{
A key that is not present returns \code{default} (\code{NULL} unless you say
otherwise). That is unambiguous: a \emph{stored} zero-length value comes back as
\code{raw(0)}, which is not \code{NULL}, so absence and emptiness stay distinguishable.

By default the stored bytes are decoded as UTF-8 text, so a value written as
a string comes back as one. MDBX records no type, so this is an assumption
rather than something the database knows: \strong{pass \code{as = "raw"} for any value
that is not text}, including anything written with \code{serialize()}. Decoding
raises an error rather than returning something corrupt when the bytes are
not valid UTF-8 text, so a wrong assumption is never silent.

The returned vector is a copy. 'libmdbx' hands out memory owned by the
database, valid only until the transaction ends, so nothing here points into
the memory map.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

mdbx_with_write(env, function(txn) {
  mdbx_put(txn, "answer", "42")
})

# Decoded as text by default.
mdbx_with_read(env, function(txn) {
  mdbx_get(txn, "answer")
})

# Anything that is not text needs as = "raw".
mdbx_with_read(env, function(txn) {
  mdbx_get(txn, "answer", as = "raw")
})

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_put]{mdbx_put()}}, \code{\link[=mdbx_del]{mdbx_del()}}
}
