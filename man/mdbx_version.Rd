% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/version.R
\name{mdbx_version}
\alias{mdbx_version}
\title{Version of the bundled libmdbx}
\usage{
mdbx_version()
}
\value{
A list with integer components \code{major}, \code{minor}, \code{patch} and \code{tweak};
character components \code{describe} (the upstream git-describe string) and
\code{commit}; and \code{build}, a list describing how the amalgamation was compiled
into this package — \code{datetime}, \code{target} (the cpu/arch/system triplet),
\code{compiler}, \code{options} (the 'libmdbx' build options in effect) and \code{flags}.
}
\description{
Reports the version of the vendored 'libmdbx' amalgamation that was compiled
into this package. The sources are bundled, so this describes the library
actually in use, not one found on the system.
}
\examples{
mdbx_version()$describe

# How the bundled sources were compiled here.
mdbx_version()$build$target
}
