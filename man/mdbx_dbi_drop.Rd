% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbi.R
\name{mdbx_dbi_drop}
\alias{mdbx_dbi_drop}
\title{Empty or delete a named database}
\usage{
mdbx_dbi_drop(txn, db, delete = FALSE)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}, opened for writing.}

\item{db}{An \code{mdbx_dbi} object from \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}}, or \code{NULL} for the main
database — which can be emptied but not deleted.}

\item{delete}{If \code{TRUE}, delete the database rather than just emptying it.}
}
\value{
\code{NULL}, invisibly.
}
\description{
\code{delete = FALSE} removes every record but keeps the database. \code{delete = TRUE}
removes the database itself, after which the handle refers to nothing and
reopening it needs \code{create = TRUE} again.
}
\details{
Like every other write, this takes effect only when the transaction commits.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path, max_dbs = 8)

mdbx_with_write(env, function(txn) {
  scratch <- mdbx_dbi_open(txn, "scratch", create = TRUE)
  mdbx_put(txn, "k", "v", db = scratch)
  mdbx_dbi_drop(txn, scratch)
  mdbx_keys(txn, db = scratch)
})

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}}
}
