% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{mdbx_put}
\alias{mdbx_put}
\title{Write a value}
\usage{
mdbx_put(txn, key, value, overwrite = TRUE, db = NULL)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}, opened with
\code{write = TRUE}.}

\item{key}{A raw vector, or a single string, which is stored as its UTF-8
bytes.}

\item{value}{A raw vector, or a single string, which is stored as its UTF-8
bytes. Use \code{serialize(x, NULL)} for an arbitrary R object.}

\item{overwrite}{If \code{FALSE}, leave an existing value alone and return
\code{FALSE} instead of replacing it.}

\item{db}{An \code{mdbx_dbi} object from \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} naming a database to
address instead of the unnamed main one, or \code{NULL} for the main database.}
}
\value{
\code{TRUE} if the value was stored, \code{FALSE} if \code{overwrite = FALSE} and
the key already existed. Returned invisibly.
}
\description{
Stores \code{value} under \code{key}, replacing any existing value unless \code{overwrite}
is \code{FALSE}.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

mdbx_with_write(env, function(txn) {
  mdbx_put(txn, "k", "first")

  # Refuses to replace, and says so.
  mdbx_put(txn, "k", "second", overwrite = FALSE)
})

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_get]{mdbx_get()}}, \code{\link[=mdbx_del]{mdbx_del()}}
}
