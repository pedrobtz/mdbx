% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\name{mdbx_del}
\alias{mdbx_del}
\title{Delete a key}
\usage{
mdbx_del(txn, key, db = NULL)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}, opened with
\code{write = TRUE}.}

\item{key}{A raw vector, or a single string, which is stored as its UTF-8
bytes.}

\item{db}{An \code{mdbx_dbi} object from \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} naming a database to
address instead of the unnamed main one, or \code{NULL} for the main database.}
}
\value{
\code{TRUE} if a record existed and was removed, \code{FALSE} otherwise.
Returned invisibly.
}
\description{
Removes \code{key} and its value. Deleting a key that is not present is not an
error; the return value says which happened.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

mdbx_with_write(env, function(txn) {
  mdbx_put(txn, "k", "v")
  mdbx_del(txn, "k")
})

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_get]{mdbx_get()}}, \code{\link[=mdbx_put]{mdbx_put()}}
}
