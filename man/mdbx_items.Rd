% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scan.R
\name{mdbx_items}
\alias{mdbx_items}
\title{List the keys and values in a database}
\usage{
mdbx_items(
  txn,
  limit = NULL,
  as = c("character", "raw"),
  db = NULL,
  start = NULL,
  reverse = FALSE,
  keys_as = NULL
)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object, from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}

\item{limit}{Maximum number of records to return, in key order. \code{NULL}, the
default, returns all of them, subject to the \link{mdbx_scan_max} guard;
\code{Inf} returns all of them unconditionally.}

\item{as}{\code{"character"} (the default) to decode values as UTF-8 text, or
\code{"raw"} for a list of raw vectors. Decoding fails rather than corrupting
anything, so a database holding \code{serialize()} output needs \code{as = "raw"}.
Governs keys too unless \code{keys_as} says otherwise.}

\item{db}{An \code{mdbx_dbi} object from \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} naming a database to
address instead of the unnamed main one, or \code{NULL} for the main database.}

\item{start}{Begin at this key rather than at an end: the first key at or
after it going forwards, or the last key at or before it going backwards.
A raw vector or a single string, as for \code{\link[=mdbx_get]{mdbx_get()}}.

\code{start} is \emph{inclusive}, so a key that exists is returned rather than
skipped. To walk a database in chunks, pass the last key of one call as the
\code{start} of the next and drop the first record of the result — otherwise it
repeats. \code{start} positions the cursor and nothing more: it does not bound
how much comes back, so it does not exempt a scan from
\link{mdbx_scan_max}. Pass \code{limit} for that.}

\item{reverse}{If \code{TRUE}, walk from the last key towards the first.
\code{limit = 1} with \code{reverse = TRUE} is the largest key, which is otherwise
only reachable by reading every key.}

\item{keys_as}{How to decode keys, when that differs from the values. An
index typically has binary keys — an encoded timestamp or counter — and
text values, which is \verb{as = "character", keys_as = "raw"}.}
}
\value{
A list with two parallel components, \code{keys} and \code{values}, each a
character vector or a list of raw vectors according to \code{as}.
}
\description{
Walks the whole database in key order and returns both keys and values, in
one pass. Use \code{\link[=mdbx_keys]{mdbx_keys()}} instead when you do not need the values —
fetching them is the expensive part.
}
\details{
The two components are parallel: \code{keys[[i]]} names \code{values[[i]]}. When keys
are text, \code{setNames(items$values, items$keys)} turns the result into a
lookup list.

The caution in \code{\link[=mdbx_keys]{mdbx_keys()}} about materializing everything applies here with
more force, since values are usually larger than keys.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

mdbx_with_write(env, function(txn) {
  mdbx_put(txn, "a", "1")
  mdbx_put(txn, "b", "2")
})

items <- mdbx_with_read(env, function(txn) mdbx_items(txn))
items

# A lookup list, when the keys are text.
stats::setNames(items$values, items$keys)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_keys]{mdbx_keys()}}, \code{\link[=mdbx_get]{mdbx_get()}}
}
