% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/txn.R
\name{mdbx_txn_state}
\alias{mdbx_txn_state}
\title{State of a transaction}
\usage{
mdbx_txn_state(txn)
}
\arguments{
\item{txn}{An \code{mdbx_txn} object, from \code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}
}
\value{
One of \code{"active"}, \code{"committed"}, or \code{"aborted"}. \code{"poisoned"} is
reported for a transaction abandoned after a 'libmdbx' assertion failure,
and \code{"invalid"} for one whose handle has already been reclaimed.
}
\description{
State of a transaction
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

txn <- mdbx_txn_begin(env)
mdbx_txn_state(txn)
mdbx_txn_abort(txn)
mdbx_txn_state(txn)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}
}
