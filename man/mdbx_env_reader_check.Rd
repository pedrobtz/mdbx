% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{mdbx_env_reader_check}
\alias{mdbx_env_reader_check}
\title{Reclaim reader slots from processes that died}
\usage{
mdbx_env_reader_check(env)
}
\arguments{
\item{env}{An \code{mdbx_env} object, from \code{\link[=mdbx_env_open]{mdbx_env_open()}}.}
}
\value{
The number of stale slots that were released, invisibly. Zero when
every occupied slot belongs to a live process.
}
\description{
A process holding a read transaction occupies a slot in the environment's
reader table. If it exits without ending the transaction — killed, crashed,
or \code{SIGKILL}ed — the slot stays occupied. Enough of those and new readers
fail with \code{MDBX_READERS_FULL} even though nothing is actually reading.
}
\details{
This asks 'libmdbx' to check every occupied slot and release the ones whose
owning process is gone. It is safe to call at any time and costs nothing when
there is nothing to reclaim, so it is a reasonable thing to run when opening
a long-lived environment that other processes also use.

Raising \code{\link[=mdbx_env_open]{mdbx_env_open()}}'s \code{max_readers} makes the table bigger; this makes
room in the table you have. See \link{mdbx-concurrency}.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path)

# Nothing has died, so nothing is reclaimed.
mdbx_env_reader_check(env)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_env_info]{mdbx_env_info()}}, which reports \code{numreaders} and \code{maxreaders}.
}
