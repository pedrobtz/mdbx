% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flags.R
\name{mdbx_env_get_flags}
\alias{mdbx_env_get_flags}
\title{Flags an environment is using}
\usage{
mdbx_env_get_flags(env)
}
\arguments{
\item{env}{An \code{mdbx_env} object, from \code{\link[=mdbx_env_open]{mdbx_env_open()}}.}
}
\value{
A character vector of flag names, empty if the environment is at
'libmdbx''s defaults in every respect.
}
\description{
Reports every flag in effect on an open environment, including the ones
\code{\link[=mdbx_env_open]{mdbx_env_open()}} set from its own \code{readonly} and \code{subdir} arguments — this
describes the environment, not the call that made it.
}
\details{
Flags can appear that were never asked for. 'libmdbx' normalizes the sync
modes, so both \code{"SAFE_NOSYNC"} and \code{"UTTERLY_NOSYNC"} also report
\code{"NOMETASYNC"} — the weaker relaxation is implied by the stronger one, and
clearing the stronger one does not clear it. \code{"UTTERLY_NOSYNC"} does \emph{not}
additionally report \code{"SAFE_NOSYNC"}, whose bits it contains: naming both
would describe one durability mode as two. An environment another process
opened first may also carry flags this one did not ask for; that is what
\code{"ACCEDE"} is about.

'libmdbx' keeps internal state in the same word, which is not reported here:
only the names in \code{\link[=mdbx_flags]{mdbx_flags()}} are ever returned.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path, flags = "NOMETASYNC")

mdbx_env_get_flags(env)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_flags]{mdbx_flags()}}, \code{\link[=mdbx_env_set_flags]{mdbx_env_set_flags()}}
}
