% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stat.R
\name{mdbx_env_info}
\alias{mdbx_env_info}
\title{Environment information}
\usage{
mdbx_env_info(x, ...)
}
\arguments{
\item{x}{An \code{mdbx_env} from \code{\link[=mdbx_env_open]{mdbx_env_open()}}, or an \code{mdbx_txn} from
\code{\link[=mdbx_txn_begin]{mdbx_txn_begin()}}.}

\item{...}{Unused, for extensibility.}
}
\value{
A named list of numbers. The \verb{geo_*} entries are the datafile
geometry — \code{geo_lower} and \code{geo_upper} are the bounds the map may grow
between (\code{geo_upper} is what \code{map_size} sets in \code{\link[=mdbx_env_open]{mdbx_env_open()}}),
\code{geo_current} its present size, and \code{geo_shrink} / \code{geo_grow} the steps it
changes by. Also \code{mapsize}, \code{file_size}, \code{last_pgno}, \code{recent_txnid},
\code{latter_reader_txnid}, \code{maxreaders}, \code{numreaders}, \code{pagesize} and
\code{sys_pagesize}.

This is a useful subset, not the whole of 'libmdbx''s \code{MDBX_envinfo}; the
omitted fields are meta-page signatures, boot ids, page-operation counters
and sync timings, which are diagnostics for 'libmdbx' itself.
}
\description{
Reports the environment as a whole: the size limits it was opened with, how
much of the map is in use, the most recent transaction, and reader slots.
}
\details{
The choice between an environment and a transaction behaves as described for
\code{\link[=mdbx_env_stat]{mdbx_env_stat()}}.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path, map_size = 4 * 1024^2)

info <- mdbx_env_info(env)
info$geo_upper
info$numreaders

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_env_stat]{mdbx_env_stat()}}
}
