% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/env.R
\name{mdbx_env_open}
\alias{mdbx_env_open}
\title{Open an MDBX environment}
\usage{
mdbx_env_open(
  path,
  readonly = FALSE,
  create = TRUE,
  subdir = FALSE,
  max_dbs = 16L,
  map_size = NULL,
  max_readers = NULL,
  mode = "0664",
  flags = NULL
)
}
\arguments{
\item{path}{Path to the environment. With \code{subdir = FALSE} (the default) this
is the data file itself, and the lock file is the same path with \code{-lck}
appended. With \code{subdir = TRUE} it is a directory, which libmdbx populates
with \code{mdbx.dat} and \code{mdbx.lck}.}

\item{readonly}{If \code{TRUE}, open for reading only; no write transaction can be
started, and the environment must already exist.}

\item{create}{If \code{FALSE}, require the environment to exist already rather
than creating it. This is a best-effort check rather than an atomic one:
libmdbx has no "open but never create" flag, so the existence test and the
open are two steps, and another process deleting the database in between
would leave a fresh one created here.}

\item{subdir}{Selects the on-disk layout described under \code{path}. It applies
only when creating; opening an existing environment detects the layout.}

\item{max_dbs}{How many named databases to make room for. The default of 16
is this package's, not libmdbx's: libmdbx reserves none, which makes
\code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} fail with \code{MDBX_DBS_FULL} on an environment opened with
default arguments. Unused slots cost nothing. Each name that

Each name that \code{\link[=mdbx_dbi_open]{mdbx_dbi_open()}} resolves occupies one slot. Raise this if
you need more than 16, or pass \code{NULL} to take libmdbx's default of none —
which leaves only the unnamed main database usable.}

\item{map_size}{Upper bound, in bytes, on the size the memory map may grow
to, or \code{NULL} for the libmdbx default. Only the upper bound is set; the
initial size and growth behaviour stay at their defaults.}

\item{max_readers}{Number of reader slots to make room for, or \code{NULL} for the
libmdbx default. One slot is used per process holding a read transaction,
so this is the ceiling on concurrent readers across all processes — see
\link{mdbx-concurrency}. The default is derived from the lock file's page size
(a few hundred, platform-dependent); raise it only if you expect more
concurrent reader processes than that. It sizes the lock file, so it takes
effect only for the first process to open the environment.}

\item{mode}{File permissions for a newly created database, as a string of
octal digits or an \link{octmode} object. The default \code{"0664"} is libmdbx's own,
and is masked by the process \code{umask} as usual — so it typically lands as
\code{0644}, readable by everyone. Pass \code{"0600"} for a database only its owner
can read. Ignored when the database already exists.}

\item{flags}{A character vector of 'libmdbx' flag names, or \code{NULL} for none.
These are the remaining \verb{MDBX_*} environment flags, with the prefix
dropped — \code{\link[=mdbx_flags]{mdbx_flags()}} lists them and explains what each does.

The durability flags live here: \code{"NOMETASYNC"}, \code{"SAFE_NOSYNC"} and
\code{"UTTERLY_NOSYNC"} each make commits cheaper by giving up some of what a
crash cannot take away, and the last of the three can leave the database
corrupt. The default — passing nothing — is fully durable. Read the
Durability section of \code{\link[=mdbx_flags]{mdbx_flags()}} before using any of them.}
}
\value{
An \code{mdbx_env} object.
}
\description{
Opens, and by default creates, an MDBX environment at \code{path}. An environment
is the unit that holds the memory map, the lock, and the databases within it;
transactions and all data access happen against one.
}
\details{
The environment is closed when \code{\link[=mdbx_env_close]{mdbx_env_close()}} is called on it, or when the
object is garbage collected, whichever happens first. Relying on garbage
collection is safe but not timely; close explicitly when the moment matters.
}
\examples{
path <- tempfile(fileext = ".mdbx")

env <- mdbx_env_open(path)
env

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_env_close]{mdbx_env_close()}}, \code{\link[=mdbx_env_is_open]{mdbx_env_is_open()}}, \code{\link[=mdbx_flags]{mdbx_flags()}},
\link{mdbx-concurrency}
}
