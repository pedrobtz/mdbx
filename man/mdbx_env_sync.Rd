% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/flags.R
\name{mdbx_env_sync}
\alias{mdbx_env_sync}
\title{Flush an environment to disk}
\usage{
mdbx_env_sync(env, force = TRUE, nonblock = FALSE)
}
\arguments{
\item{env}{An \code{mdbx_env} object, from \code{\link[=mdbx_env_open]{mdbx_env_open()}}.}

\item{force}{If \code{TRUE}, always flush. If \code{FALSE}, flush only if a threshold
set on the environment has been reached, which is 'libmdbx''s polling mode.}

\item{nonblock}{If \code{TRUE}, give up rather than wait when another process is
in a write transaction. The wait is signalled as an \code{MDBX_BUSY} error.}
}
\value{
Invisibly, \code{TRUE} if there was unsynced data and it was written, or
\code{FALSE} if nothing was pending.
}
\description{
Writes and flushes any data a relaxed durability mode has left outstanding.
With the default durability there is never anything to flush, because
\code{\link[=mdbx_txn_commit]{mdbx_txn_commit()}} has already done it.
}
\details{
Under \code{"SAFE_NOSYNC"} this also establishes a new steady commit point, which
is what lets 'libmdbx' start reusing freed pages again — so it bounds file
growth as well as making data durable.
}
\examples{
path <- tempfile(fileext = ".mdbx")
env <- mdbx_env_open(path, flags = "SAFE_NOSYNC")

mdbx_with_write(env, function(txn) mdbx_put(txn, "k", "v"))

# The commit above flushed nothing; this does.
mdbx_env_sync(env)

mdbx_env_close(env)
unlink(c(path, paste0(path, "-lck")))
}
\seealso{
\code{\link[=mdbx_flags]{mdbx_flags()}}, \code{\link[=mdbx_env_set_flags]{mdbx_env_set_flags()}}
}
